export const metadata = {
  title: "Craque ou Bagre",
  description: "Painel administrativo do Craque ou Bagre",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
